name: main_comparison
description: Main comparison: all models × all horizons × 10 seeds × 5 folds

models:
  - hpqrc
  - qrc_baseline
  - photonic_only
  - esn
  - lstm
  - transformer

data: acn_caltech

horizons: [1, 6, 24, 72, 168]

n_seeds: 10
n_folds: 5

save_results: true
save_models: false
