# Memory Capacity Benchmark

name: "memory_capacity"
description: "Run IPC/STM/NMC benchmarks"

models:
  - "hpqrc"
  - "qrc_baseline"
  - "photonic_only"
  - "esn"

max_delay: 200
max_degree: 3

dataset: "synthetic"

output_dir: "./outputs/memory"
